name: Test core functionality of image

on:
  workflow_call:
    inputs:
      runner:
        required: true
        type: string
      image_os:
        required: true
        type: string
      arch:
        required: true
        type: string

jobs:
  test_image:
    uses: ./.github/workflows/test_image.yml
    with:
      runner: ${{ inputs.runner }}
      image_os: ${{ inputs.image_os }}
      arch: ${{ inputs.arch }}

  test_cache:
    uses: ./.github/workflows/test_cache.yml
    with:
      runner: ${{ inputs.runner }}
      image_os: ${{ inputs.image_os }}
      arch: ${{ inputs.arch }}
